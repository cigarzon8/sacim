{{> layout}}
{{> header}}
{{> nav}}
<div class="container-xx3">
<a href="/proyecto/add"><td><button class="btn btn-primary">Agregar</button></td></a>

<table class="table">
  <thead>
    <tr>
    {{#each headerlist}}
        <th scope="row">{{this}}</th>
    {{/each}}
    </tr>
  </thead>
  <tbody>
    {{#each datalist}}
        <tr>
        <td>{{this.id}}</td>
        <td>{{this.nombre}}</td>
        <td>{{this.estado}}</td>
        <td>{{this.createdAt}}</td>
        <td>
          <a href="/proyecto/remove/{{this.id}}">
            <button class="btn btn-danger">Delete</button>
          </a>
        </td>
        </tr>
    {{/each}}

  </tbody>
</table>

</div>

{{> footer}}